/* Koisa Linda — GA4 single-source configuration.
 * Fill only after the official GA4 web data stream is created.
 * Expected format: prefix G, hyphen, then the official stream identifier.
 */
window.KL_GA4_MEASUREMENT_ID = "";
